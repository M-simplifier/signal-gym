# Contributing

Signal Gym is early and issue-first.

Before opening a pull request, please open an issue describing the change and the claim boundary it affects. Changes that add health claims, telemetry, accounts, or remote AI calls need explicit maintainer approval first.

Local checks:

```sh
npm install
npm run verify
```

Keep public text conservative. This project is a training prototype, not a validated medical or intelligence-enhancement product.
